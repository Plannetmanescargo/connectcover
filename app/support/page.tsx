export { default } from "../help-support/page";
import { documentMetadata } from "@/components/documents/Pages";
export const metadata = documentMetadata(
  "Document support",
  "Help with your automotive documents.",
  "/support",
);
